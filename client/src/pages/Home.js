import React from 'react';
import { useQuery } from '@apollo/client';

const Home = () => {
  <main>
    <h1>This is the home page.</h1>
    <p>Pretty neat, huh?</p>
  </main>
}

export default Home;
